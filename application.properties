# spring.datasource.url=jdbc:mysql://localhost:3306/testtwo?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# application.properties

# 服务器配置（必须）
server.port=${PORT:8080}
server.address=0.0.0.0

# Railway数据库连接（自动注入，直接使用）
spring.datasource.url=mysql://root:UgWREIpFqsMlnfdnCyTLUOCkvsycfkGN@yamabiko.proxy.rlwy.net:10678/railway
spring.datasource.driver-class-name=org.postgresql.Driver # MySQL用com.mysql.cj.jdbc.Driver
spring.datasource.username=${PGUSER} # Railway自动注入的数据库用户名
spring.datasource.password=${PGPASSWORD} # Railway自动注入的数据库密码

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# 连接池优化
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.idle-timeout=30000